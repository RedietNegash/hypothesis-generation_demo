CELL_TYPES = [
    "Adipocyte",
    "Airway_Goblet_Cell",
    "Alveolar_Capillary_Endothelial_Cell",
    "Alveolar_Type_1_AT1_Cell",
    "Alveolar_Type_2_AT2_Cell",
    "Alverolar_Type_2,Immune",
    "Astrocyte_1",
    "Astrocyte_2",
    "Atrial_Cardiomyocyte",
    "Basal_Epidermal_Skin",
    "Basal_Epithelial_Mammary",
    "Blood_Brain_Barrier_Endothelial_Cell",
    "CNS,Enteric_Neuron",
    "Cardiac_Fibroblasts",
    "Cardiac_Pericyte_1",
    "Cardiac_Pericyte_2",
    "Cardiac_Pericyte_3",
    "Cardiac_Pericyte_4",
    "Chief_Cell",
    "Cilliated_Cell",
    "Club_Cell",
    "Colon_Epithelial_Cell_1",
    "Colon_Epithelial_Cell_2",
    "Colon_Epithelial_Cell_3",
    "Colonic_Goblet_Cell",
    "Cortical_Epithelial-like",
    "Ductal_Cell_Pancreatic",
    "Eccrine_Epidermal_Skin",
    "Endocardial_Cell",
    "Endothelial_Cell_General_1",
    "Endothelial_Cell_General_2",
    "Endothelial_Cell_General_3",
    "Endothelial_Cell_Myocardial",
    "Endothelial_Exocrine_Tissues",
    "Enterochromaffin_Cell",
    "Esophageal_Epithelial_Cell",
    "Fetal_Adrenal_Chromaffin_Cell",
    "Fetal_Adrenal_Cortical_Cell",
    "Fetal_Adrenal_Neuron",
    "Fetal_Adrenal_Sympathoblasts",
    "Fetal_Alveolar_Endothelial_Cell",
    "Fetal_Astrocyte_1",
    "Fetal_Astrocyte_2",
    "Fetal_Astrocyte_3",
    "Fetal_Astrocyte_4",
    "Fetal_Astrocyte_5",
    "Fetal_Atrial_Cardiomyocyte",
    "Fetal_B_Lymphocyte_1_SPIB+",
    "Fetal_B_Lymphocyte_2_CXCR5+",
    "Fetal_B_Lymphocyte_3_NPY+",
    "Fetal_Broncial_and_Alveolar_Epithelial_Cell_1",
    "Fetal_Broncial_and_Alveolar_Epithelial_Cell_2",
    "Fetal_Cardiac_Fibroblast",
    "Fetal_Cholangiocytes",
    "Fetal_Cilliated_Epithelial_Cell",
    "Fetal_Endocardial_Cell",
    "Fetal_Endothelial_General_1",
    "Fetal_Endothelial_General_2",
    "Fetal_Endothelial_General_3",
    "Fetal_Enteric_Glia",
    "Fetal_Enteric_Neuron",
    "Fetal_Enterocyte_1",
    "Fetal_Enterocyte_2",
    "Fetal_Enterocyte_3",
    "Fetal_Enteroendocrine",
    "Fetal_Erythroblast_1",
    "Fetal_Erythroblast_2",
    "Fetal_Erythroblast_3",
    "Fetal_Erythroblast_4",
    "Fetal_Erythroblast_5",
    "Fetal_Excitatory_Neuron_1",
    "Fetal_Excitatory_Neuron_2",
    "Fetal_Excitatory_Neuron_3",
    "Fetal_Excitatory_Neuron_4",
    "Fetal_Excitatory_Neuron_5",
    "Fetal_Excitatory_Neuron_6",
    "Fetal_Excitatory_Neuron_7",
    "Fetal_Excitatory_Neuron_8",
    "Fetal_Excitatory_Neuron_9",
    "Fetal_Excitatory_Neuron_10",
    "Fetal_Excitatory_Neuron_11",
    "Fetal_Excitatory_Neuron_12",
    "Fetal_Extravillous_Trophoblast",
    "Fetal_Fibroblast_Gastrointestinal",
    "Fetal_Fibroblast_General_1",
    "Fetal_Fibroblast_General_2",
    "Fetal_Fibroblast_General_3",
    "Fetal_Fibroblast_General_4",
    "Fetal_Fibroblast_General_5",
    "Fetal_Fibroblast_Sk_Muscle_Associated_1",
    "Fetal_Fibroblast_Splenic",
    "Fetal_Gastric_Goblet_Cell",
    "Fetal_Goblet_Cell",
    "Fetal_Hematopoeitic_Stem_Cell",
    "Fetal_Hepatic_Endothelial_1",
    "Fetal_Hepatic_Endothelial_2",
    "Fetal_Hepatic_Macrophage_1",
    "Fetal_Hepatic_Macrophage_2",
    "Fetal_Hepatic_Macrophage_3",
    "Fetal_Hepatic_Stellate_Cell",
    "Fetal_Hepatoblast",
    "Fetal_Inhibitory_Neuron_1",
    "Fetal_Inhibitory_Neuron_2",
    "Fetal_Inhibitory_Neuron_3",
    "Fetal_Inhibitory_Neuron_4",
    "Fetal_Inhibitory_Neuron_5",
    "Fetal_Lymphatic_Endothelial_Cell",
    "Fetal_MacrophageGeneral_2",
    "Fetal_Macrophage_General_1",
    "Fetal_Macrophage_General_3",
    "Fetal_Macrophage_General_4",
    "Fetal_Megakaryocyte",
    "Fetal_Mesangial_Cell_1",
    "Fetal_Mesangial_Cell_2",
    "Fetal_Mesothelial_Cell",
    "Fetal_Metanephric_Cell",
    "Fetal_Oligodendrocyte_Progenitor_2",
    "Fetal_Pancreatic_Acinar_Cell_1",
    "Fetal_Pancreatic_Acinar_Cell_2",
    "Fetal_Pancreatic_Ductal_Cell",
    "Fetal_Pancreatic_Islet_Cell",
    "Fetal_Parietal,Chief_Cell",
    "Fetal_Photoreceptor_Cell",
    "Fetal_Placental_Endothelial_Cell",
    "Fetal_Placental_Fibroblast_1",
    "Fetal_Placental_Fibroblast_2",
    "Fetal_Placental_Macrophage",
    "Fetal_Placental_Neuron",
    "Fetal_Pulmonary_Neuroendocrine_Cell",
    "Fetal_Retinal_Neuron",
    "Fetal_Retinal_Pigment_Cell",
    "Fetal_Retinal_Progenitors_and_Muller_glia_1",
    "Fetal_Retinal_Progenitors_and_Muller_glia_2",
    "Fetal_Satellite_Cell_1",
    "Fetal_Satellite_Cell_2",
    "Fetal_Schwann_Cell",
    "Fetal_Skeletal_Myocyte_1",
    "Fetal_Skeletal_Myocyte_2",
    "Fetal_Skeletal_Myocyte_3",
    "Fetal_Syncitiotrophoblast,Cytotrophoblast,Trophoblast_Giant",
    "Fetal_T_Lymphocyte_1_CD4+",
    "Fetal_T_Lymphocyte_2_Cytotoxic",
    "Fetal_T_Lymphocyte_3_IL2+",
    "Fetal_T_Lymphocyte_4_FASLG+",
    "Fetal_Thymocyte",
    "Fetal_Ureteric_Bud_Cell",
    "Fetal_Ventricular_Cardioyocyte",
    "Fibroblast_Epithelial",
    "Fibroblast_Gastrointestinal",
    "Fibroblast_General",
    "Fibroblast_Liver_Adrenal",
    "Fibroblast_Peripheral_Nerve",
    "Fibroblast_Sk_Muscle_Associated",
    "Foveolar_Cell",
    "GABAergic_Neuron_1",
    "GABAergic_Neuron_2",
    "Gastric_Neuroendocrine_Cell",
    "Glutaminergic_Neuron_1",
    "Glutaminergic_Neuron_2",
    "Granular_Epidermal_Skin",
    "Hepatocyte",
    "Keratinocyte_1",
    "Keratinocyte_2",
    "Luteal_Cell_Ovarian",
    "Lymphatic_Endothelial_Cell",
    "Macrophage_General,Alveolar",
    "Macrophage_General",
    "Mammary_Epithelial",
    "Mammary_Luminal_Epithelial_Cell_1",
    "Mammary_Luminal_Epithelial_Cell_2",
    "Mast_Cell",
    "Melanocyte",
    "Memory_B_Cell",
    "Mesothelial_Cell",
    "Microglia",
    "Myoepithelial_Skin",
    "Naive_T_cell",
    "Natural_Killer_T_Cell",
    "Oligodendrocyte",
    "Oligodendrocyte_Precursor",
    "Pancreatic_Acinar_Cell",
    "Pancreatic_Alpha_Cell_1",
    "Pancreatic_Alpha_Cell_2",
    "Pancreatic_Beta_Cell_1",
    "Pancreatic_Beta_Cell_2",
    "Pancreatic_Delta,Gamma_cell",
    "Paneth_Cell",
    "Parietal_Cell",
    "Pericyte_Esophageal_Muscularis",
    "Pericyte_General_1",
    "Pericyte_General_2",
    "Pericyte_General_3",
    "Pericyte_General_4",
    "Peripheral_Nerve_Stromal",
    "Plasma_Cell",
    "Satellite_Cell",
    "Schwann_Cell_General",
    "Small_Intestinal_Enterocyte",
    "Small_Intestinal_Goblet_Cell",
    "Smooth_Muscle_Colon_1",
    "Smooth_Muscle_Colon_2",
    "Smooth_Muscle_Esophageal_Mucosal",
    "Smooth_Muscle_Esophageal_Muscularis_1",
    "Smooth_Muscle_Esophageal_Muscularis_2",
    "Smooth_Muscle_Esophageal_Muscularis_3",
    "Smooth_Muscle_GE_Junction",
    "Smooth_Muscle_General",
    "Smooth_Muscle_General_Gastrointestinal",
    "Smooth_Muscle_Uterine",
    "Smooth_Muscle_Vaginal",
    "T_Lymphocyte_1_CD8+",
    "T_lymphocyte_2_CD4+",
    "Thyroid_Follicular_Cell",
    "Transitional_Zone_Cortical_Cell",
    "Tuft_Cell",
    "Type_II_Skeletal_Myocyte",
    "Type_I_Skeletal_Myocyte",
    "Vascular_Smooth_Muscle_1",
    "Vascular_Smooth_Muscle_2",
    "Ventricular_Cardiomyocyte",
    "Zona_Fasciculata_Cortical_Cell",
    "Zona_Glomerulosa_Cortical_Cell",
]


import os
import subprocess

import numpy as np
import pandas as pd
import pyranges as pr
from statsmodels.stats.multitest import fdrcorrection

CATLAS_BASE_URL = "https://catlas.org/humanenhancer/data/cCREs"
GENOME_BP = 3_100_000_000


def download(url: str, dest_path: str, description: str) -> bool:
    if os.path.exists(dest_path) and os.path.getsize(dest_path) > 1000:
        print(f"  Already exists: {dest_path}")
        return True
    print(f"  Downloading {description}...")
    result = subprocess.run(
        ["wget", "--continue", "--tries=5", "--timeout=120", "-O", dest_path, url]
    )
    ok = result.returncode == 0
    if ok:
        print(f"  Done: {dest_path} ({os.path.getsize(dest_path) / 1e6:.0f} MB)")
    return ok


def download_catlas_celltypes(catlas_dir: str, cell_types=None) -> list:
    """Download all (or a subset of) CATlas per-cell-type BED files. Skips files
    already on disk. Returns the list of cell types that downloaded successfully."""
    os.makedirs(catlas_dir, exist_ok=True)
    cell_types = cell_types or CELL_TYPES
    print(
        f"Downloading {len(cell_types)} CATlas cell-type BED files "
        f"(skips ones already on disk)..."
    )
    failed = []
    for i, cell_type in enumerate(cell_types):
        url = f"{CATLAS_BASE_URL}/{cell_type}.bed"
        dest = f"{catlas_dir}/{cell_type}.bed"
        ok = download(url, dest, f"CATlas {cell_type} cCREs")
        if not ok or not os.path.exists(dest) or os.path.getsize(dest) < 100:
            print(f"  FAILED: {cell_type}")
            failed.append(cell_type)
            if os.path.exists(dest):
                os.remove(dest)
        if (i + 1) % 25 == 0:
            print(f"  ...{i + 1}/{len(cell_types)}")
    if failed:
        print(f"\n{len(failed)} cell types failed to download: {failed}")
    ready = [c for c in cell_types if c not in failed]
    print(f"\n{len(ready)} cell types ready.")
    return ready


_peak_cache = {}


def get_celltype_peaks(cell_type: str, catlas_dir: str):

    cache_key = (catlas_dir, cell_type)
    if cache_key in _peak_cache:
        return _peak_cache[cache_key]
    path = f"{catlas_dir}/{cell_type}.bed"
    if not os.path.exists(path):
        _peak_cache[cache_key] = None
        return None
    bed = pd.read_csv(
        path, sep="\t", header=None, usecols=[0, 1, 2], names=["chrom", "start", "end"]
    )
    peaks = pr.PyRanges(
        chromosomes=bed["chrom"], starts=bed["start"], ends=bed["end"]
    ).merge()
    _peak_cache[cache_key] = peaks
    return peaks


def build_variant_pyranges(
    df_variants: pd.DataFrame, keep_idx: bool = True
) -> pr.PyRanges:

    chrom = df_variants["chr"].apply(
        lambda c: f"chr{c}" if not str(c).startswith("chr") else c
    )
    data = {
        "Chromosome": chrom.values,
        "Start": (df_variants["pos"] - 1).values,
        "End": df_variants["pos"].values,
    }
    if keep_idx:
        data["variant_idx"] = df_variants.index.values
    return pr.PyRanges(pd.DataFrame(data))


def generate_local_permutations(
    df_variants: pd.DataFrame, n_perm: int, window: int, seed: int = 0
) -> pr.PyRanges:

    rng = np.random.default_rng(seed)
    chrom = (
        df_variants["chr"]
        .apply(lambda c: f"chr{c}" if not str(c).startswith("chr") else c)
        .values
    )
    pos = df_variants["pos"].values

    chroms_rep, starts_rep, ends_rep, perm_ids = [], [], [], []
    for p in range(1, n_perm + 1):
        offsets = rng.integers(-window, window + 1, size=len(pos))
        new_pos = np.clip(pos + offsets, 1, None)
        chroms_rep.append(chrom)
        starts_rep.append(new_pos - 1)
        ends_rep.append(new_pos)
        perm_ids.append(np.full(len(pos), p))

    perm_df = pd.DataFrame(
        {
            "Chromosome": np.concatenate(chroms_rep),
            "Start": np.concatenate(starts_rep),
            "End": np.concatenate(ends_rep),
            "perm_id": np.concatenate(perm_ids),
        }
    )
    return pr.PyRanges(perm_df)


def compute_celltype_breadth(
    df_variants: pd.DataFrame, catlas_dir: str, cell_types=None
) -> pd.DataFrame:

    cell_types = cell_types or CELL_TYPES
    all_peaks_rows = []
    for ct in cell_types:
        peaks = get_celltype_peaks(ct, catlas_dir)
        if peaks is None:
            continue
        all_peaks_rows.append(
            pd.DataFrame(
                {
                    "Chromosome": peaks.Chromosome,
                    "Start": peaks.Start,
                    "End": peaks.End,
                    "cell_type": ct,
                }
            )
        )
    all_peaks_pr = pr.PyRanges(pd.concat(all_peaks_rows, ignore_index=True))

    variant_pr = build_variant_pyranges(df_variants, keep_idx=True)
    joined = variant_pr.join(all_peaks_pr)
    breadth = pd.Series(joined.variant_idx).value_counts()

    df_out = df_variants.copy()
    df_out["n_celltypes_accessible"] = df_out.index.map(breadth).fillna(0).astype(int)
    df_out["pct_celltypes_accessible"] = df_out["n_celltypes_accessible"] / len(
        cell_types
    )
    return df_out


def filter_promiscuous_variants(
    df_variants: pd.DataFrame,
    catlas_dir: str,
    threshold: float,
    label: str = "",
    cell_types=None,
) -> pd.DataFrame:
    df_scored = compute_celltype_breadth(df_variants, catlas_dir, cell_types)
    df_filtered = df_scored[df_scored["pct_celltypes_accessible"] <= threshold].copy()
    print(
        f"[{label}] Excluded {len(df_scored) - len(df_filtered)}/{len(df_scored)} "
        f"promiscuous variants (>{threshold:.0%} of cell types) -- {len(df_filtered)} remain"
    )
    return df_filtered


def screen_celltypes(
    df_variants: pd.DataFrame,
    catlas_dir: str,
    label: str = "",
    n_perm: int = 100_000,
    window: int = 1_000_000,
    seed: int = 0,
    cell_types=None,
) -> pd.DataFrame:

    cell_types = cell_types or CELL_TYPES
    variant_pr = build_variant_pyranges(df_variants, keep_idx=False)
    perm_pr = generate_local_permutations(
        df_variants, n_perm=n_perm, window=window, seed=seed
    )
    n_total = len(df_variants)
    results = []

    for cell_type in cell_types:
        peak_pr = get_celltype_peaks(cell_type, catlas_dir)
        if peak_pr is None:
            continue

        n_in = len(variant_pr.overlap(peak_pr))
        if n_in == 0:
            continue

        ov = perm_pr.overlap(peak_pr)
        null_counts = (
            pd.Series(ov.perm_id)
            .value_counts()
            .reindex(range(1, n_perm + 1), fill_value=0)
            .values
            if len(ov)
            else np.zeros(n_perm)
        )

        p_perm = (np.sum(null_counts >= n_in) + 1) / (n_perm + 1)
        null_mean = null_counts.mean()
        enrichment = n_in / null_mean if null_mean > 0 else np.inf

        results.append(
            {
                "cell_type": cell_type,
                "n_variants_in_peaks": n_in,
                "n_total_variants": n_total,
                "null_mean_in_peaks": round(float(null_mean), 2),
                "enrichment_ratio": round(float(enrichment), 3)
                if np.isfinite(enrichment)
                else enrichment,
                "p_perm": p_perm,
            }
        )

    cols = [
        "cell_type",
        "n_variants_in_peaks",
        "n_total_variants",
        "null_mean_in_peaks",
        "enrichment_ratio",
        "p_perm",
    ]
    results_df = (
        pd.DataFrame(results, columns=cols).sort_values("p_perm")
        if results
        else pd.DataFrame(columns=cols)
    )
    if len(results_df):
        _, results_df["FDR"] = fdrcorrection(results_df["p_perm"])
    print(
        f"[{label}] Tested {len(results_df)}/{len(cell_types)} cell types "
        f"({len(cell_types) - len(results_df)} had zero overlapping variants), "
        f"n_perm={n_perm}, window=+/-{window:,}bp"
    )
    return results_df


def get_shortlist(
    results_df: pd.DataFrame, fdr_threshold: float = 0.10, top_n_fallback: int = 15
) -> list:

    if len(results_df) == 0:
        print("No cell types tested -- nothing to shortlist.")
        return []
    shortlist = results_df.loc[results_df["FDR"] < fdr_threshold, "cell_type"].tolist()
    if shortlist:
        print(
            f"Shortlisted {len(shortlist)} cell types at FDR < {fdr_threshold}: {shortlist}"
        )
        return shortlist
    fallback = (
        results_df.sort_values("p_perm").head(top_n_fallback)["cell_type"].tolist()
    )
    print(
        f"No cell types passed FDR < {fdr_threshold} -- exporting top {len(fallback)} "
        f"by rank instead (exploratory only, not statistically confirmed): {fallback}"
    )
    return fallback
